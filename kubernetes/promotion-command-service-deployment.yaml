apiVersion: apps/v1
kind: Deployment
metadata:
  name: promotion-command-service
  namespace: retail-promotion-platform
  labels:
    app: promotion-command-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: promotion-command-service
  template:
    metadata:
      labels:
        app: promotion-command-service
    spec:
      containers:
        - name: promotion-command-service
          image: retailplatformacr.azurecr.io/promotion-command-service:v1
          imagePullPolicy: Never
          ports:
            - containerPort: 8082
          env:
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://discovery-server:8761/eureka/"
            - name: SPRING_CONFIG_IMPORT
              value: "optional:configserver:http://config-server:8888"
          resources:
            requests:
              cpu: "250m"
              memory: "384Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 45
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: promotion-command-service
  namespace: retail-promotion-platform
spec:
  selector:
    app: promotion-command-service
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP
